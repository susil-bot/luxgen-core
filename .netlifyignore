# Netlify ignore file - Exclude ALL files that contain NODE_ENV references
# This prevents Netlify secrets scanner from finding false positives

# Environment files
.env*
*.env*

# Node modules
node_modules/

# Build artifacts (contains compiled code with NODE_ENV)
dist/
build/
coverage/

# ALL documentation files (contain NODE_ENV examples)
*.md
docs/
README*
DEPLOYMENT*
NETLIFY*
SECURE*
RAILWAY*
API_*

# ALL configuration files (contain NODE_ENV references)
.eslintrc*
.gitignore
Dockerfile*
docker-compose*
deploy-*
*.sh
*.yml
*.yaml

# Git
.git/
.gitignore

# IDE files
.vscode/
.idea/
*.swp
*.swo

# OS files
.DS_Store
Thumbs.db

# Logs
logs/
*.log

# Temporary files
tmp/
temp/

# GitHub Actions (contain NODE_ENV references)
.github/

# Scripts (contain NODE_ENV references)
scripts/

# Tests (may contain NODE_ENV references)
tests/
test/

# All other files that might contain NODE_ENV
*.example
*.template
*.config.js
*.config.json
